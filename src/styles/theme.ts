import { Theme } from '@emotion/react';

const colors = {
  brand: '#FF4600',
  black: '#292929',
  grey: '#9E9E9E',
  lightGrey: '#FAFAFA',
  white: '#fff'
};

const tone = {
  light: '#fff',
  dark: '#232323'
};

const theme: Theme = {
  colors,
  tone
};

export default theme;
